#include "main.h"
/**
 * _isupper - detect cases in alphabets
 *
 * @c: input parameter
 *
 * Return: (1) for true (0) otherwise
 *
 */
int _isupper(int c)
{
if (c >= 'A' && c <= 'Z')
{
return (1);
}
else
{
return (0);
}
}
