#include "main.h"
/**
 * _abs - computes the absolute value of an integer
 *
 * description: it uses an argument of type n
 *
 * @n: it is the argument used to determine return type
 *
 * Return: (-n)
 *
 */
int _abs(int n)
{
if (n >= 0)
{
return (n);
}
else
{
return (-n);
}
}
